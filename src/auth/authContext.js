import { createContext } from "react";

// el contexto provee una forma de compartir información a lo largo de los componentes
// el createContext genera un componente
export const AuthContext = createContext();
